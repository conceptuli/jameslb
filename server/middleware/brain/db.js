// Generated by CoffeeScript 1.8.0
(function() {
  var BrainDb, app, db, dbName, fs, loki, loopback, path, string;

  loopback = require('loopback');

  app = loopback();

  loki = require('lokijs');

  fs = require('fs');

  path = require('path');

  BrainDb = function(dbName) {
    dbName = new loki("" + dbName + ".json");
    dbName.saveToDisk();
    return dbName.on('addCollection', function(collectionName) {
      return this.createCollection(collectionName);
    });
  };

  dbName = 'foo';

  console.log(db = new BrainDb(dbName));

  db.addCollection(string = 'test');


  /*
  
  dbName = footon "#{dbName}"
  dbName.on 'ready', ->
    console.log 'ready'
   */

}).call(this);

//# sourceMappingURL=db.js.map
